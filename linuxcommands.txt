ls->list the files and directories
ls -l -> list the files and directories in long format
ls -a ->this command will list the hidden files and folders
ls -lh->list the files and folders in long format in human readable format